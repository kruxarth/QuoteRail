# QuoteRail evals

Labeled RFQs for requirement extraction, feasibility, and policy graders.

Live model evals are opt-in (`OPENCODE_GO_API_KEY`). CI runs the fake adapter and deterministic graders only.
